function Greet(name) {
    alert("Hello, " + name + "!");
    if (name = null) {
        alert("Hello, there!");
    }
    if (name = name.toUpperCase) {
        alert("HELLO, " + name.toUpperCase + "!");
    }
    if (name = [n]) {
        alert("Hello, " + [n]);
    }
}